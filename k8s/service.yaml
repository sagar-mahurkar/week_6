apiVersion: v1
kind: Service
metadata:
  name: gke-iris-demo-workload-service   # From table #14
spec:
  type: LoadBalancer
  selector:
    app: iris-api                         # Must match deployment label (#15)
  ports:
    - port: 80                             # External port (#17)
      targetPort: 8000                     # Container port (#16)
